<div align="center">

<img src="docs/caelo.png" width="140" alt="Caelo">

# Caelo

**Вставил ссылку — работает.**

VPN-клиент под подписки, для людей, которым не должно быть нужно разбираться, что такое
протокол.

[![Flutter](https://github.com/TeamGDB/Caelo/actions/workflows/flutter.yml/badge.svg)](https://github.com/TeamGDB/Caelo/actions/workflows/flutter.yml)
[![Core](https://github.com/TeamGDB/Caelo/actions/workflows/core.yml/badge.svg)](https://github.com/TeamGDB/Caelo/actions/workflows/core.yml)
[![License](https://img.shields.io/badge/лицензия-GPL--3.0--or--later-blue)](LICENSE)
[![Platforms](https://img.shields.io/badge/платформы-macOS%20%7C%20iOS%20%7C%20Android%20%7C%20Linux%20%7C%20Windows-lightgrey)](#платформы)
[![AmneziaWG](https://img.shields.io/badge/AmneziaWG-Jc%20%C2%B7%20S1--S4%20%C2%B7%20H1--H4%20%C2%B7%20I1--I5-2FA982)](#протоколы)

[English](README.md) · **Русский**

</div>

---

> **Ранняя разработка.** Туннель работает. Остальное не закончено, релизов пока нет.

## Что это

Без аккаунтов, рекламы и телеметрии. Свободное ПО под GPLv3.

Caelo говорит на [AmneziaWG](https://docs.amnezia.org/documentation/amnezia-wg/) с полным
набором параметров обфускации — мусорные пакеты, магические диапазоны заголовков и
сигнатурные пакеты AWG 2.0, из-за которых хендшейк выглядит как DNS, QUIC или SIP. Каким
из них ты в итоге пользуешься — вопрос, который приложение не задаёт.

Главный экран — кнопка, слово и строчка мелким шрифтом о том, что получилось. Настройки
существуют для случаев, которые автоматика не покрывает, и заходить туда не должно
требоваться.

## Протоколы

| | |
| --- | --- |
| **AmneziaWG** | `Jc` `Jmin` `Jmax` · `S1`–`S4` · `H1`–`H4` · `I1`–`I5` |
| **VLESS / REALITY** | через sing-box |
| Всё остальное, что умеет sing-box | достаётся заодно, специально не пилим |

## Платформы

| Платформа | Туннель | Накрывает |
| --- | --- | --- |
| **macOS** | привилегированный хелпер, дальше NetworkExtension | всю машину |
| **Android** | `VpnService` | всё устройство |
| **iOS** | внутри процесса, дальше расширение packet tunnel | только свой процесс |
| **Linux** | привилегированная служба из deb или rpm; иначе внутри процесса | вся машина или только процесс |
| **Windows** | — | только интерфейс |

## Архитектура

Ядро знает про конфигурации, а не про подписки. Оно умеет сказать, что внутри одной,
ходит ли через неё трафик, и поднять её — по одной за раз. Всё, что делает подписку
подпиской, — это доставка: ссылка, HTTP-запрос, интервал обновления, кэш последней
рабочей копии, заголовок с остатком трафика, несколько источников, слитых в один список.
Ничего из этого туннель не исполняет, и всё это — то, что пользователь видит и меняет,
поэтому живёт во Flutter.

Правило, которое не двигается, — состояние соединения. Оно существует ровно в одном
месте, потому что состояние, хранимое в двух, однажды разойдётся само с собой в вопросе,
что подключено.

Узлы перебираются в том порядке, который дал сервер. Измерение доступно там, где помогает:
`caelo_probe` отвечает, ходит ли через конфигурацию трафик, — на пользовательском стеке,
не трогая ни сеть машины, ни уже поднятый туннель. Но порядок — серверный, клиент его не
пересчитывает.

**Caelo не будет форкать ничего.** [sing-box](https://github.com/SagerNet/sing-box)
подключается обычным Go-модулем, а ядро регистрирует собственный эндпоинт `amneziawg`
через его публичный реестр — именно это делает AmneziaWG-узел выразимым в том же
документе, что и всё остальное. Зависимостью он пока не является: его никто не импортирует,
а версия, зафиксированная для сборки, которая её не использует, записывает намерение, а не
факт. AmneziaWG берётся из
[amneziawg-go](https://github.com/amnezia-vpn/amneziawg-go), тоже как опубликован.
Обновление обоих — бамп версии, ребейзить нечего.

Альтернативой был форк `sagernet/wireguard-go` с переносом обфускации внутрь. Если мерить
от общего апстрима, эта ветка и `amneziawg-go` разошлись сопоставимо — примерно 1700 и
1800 существенных строк, — то есть сведение их в любую сторону это настоящая работа,
причём повторяемая на каждом релизе AmneziaWG. AmneziaWG — смысл проекта, поэтому свежей
должна оставаться именно эта зависимость.

| Путь | Что там |
| --- | --- |
| [`lib/`](lib) | Интерфейс. Flutter, Cupertino, один вид везде. |
| [`core/`](core) | Go-ядро: туннель, проба, состояние. |
| [`packaging/`](packaging) | Превращение сборки в то, что ставится. |
| [`scripts/`](scripts) | По одному скрипту сборки на платформу. |

Зафиксировано: amneziawg-go `v3.0.20260805`. Намечено: sing-box `v1.13.16`.

## Сборка

Каждый скрипт сначала собирает ядро, кладёт его туда, где загрузчик этой платформы ищет
сам, и только потом собирает приложение.

```bash
./scripts/build-macos.sh debug     # универсальный dylib в Contents/Frameworks
./scripts/build-android.sh debug   # по .so на ABI, внутрь APK
./scripts/build-ios.sh release     # статический xcframework в бинарь
./scripts/build-linux.sh debug     # .so в bundle/lib
./scripts/build-windows.sh debug   # .dll рядом с exe
```

Для Android нужен NDK — скрипт находит его в SDK, о котором Flutter и так знает. Отладочные
сборки iOS используют JIT и не запускаются без подключённого тулинга, поэтому там по
умолчанию release.

Любой из них можно запустить и без ядра — `flutter build <платформа>` соберёт интерфейс.
Приложение тогда честно скажет, что ядра нет, а не сделает вид.

### Попробовать туннель без приложения

```bash
cd core && go run ./cmd/caelo-probe -config /путь/к/tunnel.conf
```

Поднимает один туннель на userspace-стеке и тащит через него URL. Без прав, без интерфейса
на хосте, ничего вне процесса не заворачивается. Если напечатанный адрес — это адрес
эндпоинта, а не твой, значит сработало.

## Пакеты

| Платформа | Форматы |
| --- | --- |
| Android | по APK на ABI, универсальный APK для сайдлоада и `.aab` |
| Linux | `.deb`, `.rpm`, AppImage, `.tar.gz` |
| macOS | `.dmg`, `.pkg` |
| Windows | установщик, портативный `.zip` |

Четыре формата для Linux, потому что «Linux» — это не одна вещь. Ничего из этого пока не
подписано и не нотаризовано, и неподписанную сборку нельзя выдавать за релиз.

## Участие

[CONTRIBUTING.md](CONTRIBUTING.md). Подписывай коммиты (`git commit -s`) и никогда не
вставляй в issue настоящую ссылку на подписку, ключ или адрес сервера.

Нашёл уязвимость? [SECURITY.md](SECURITY.md), а не публичный issue.

## Лицензия

GPL-3.0-or-later, см. [LICENSE](LICENSE).

Caelo построен на [sing-box](https://github.com/SagerNet/sing-box) и
[AmneziaWG](https://github.com/amnezia-vpn/amneziawg-go); что откуда взято — в
[ATTRIBUTION.md](ATTRIBUTION.md). Ни один из проектов не поддерживает Caelo и не связан с
ним.
